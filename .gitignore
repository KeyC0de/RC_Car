# Windows thumbnail cache files
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Dump file
*.stackdump

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows Installer files
*.cab
*.msi
*.msix
*.msm
*.msp

# Windows shortcuts
*.lnk

# exclude specific files
Read 2 Pins, write to an other pin.c
selected Led blinks continuously depending on the state of the button(polling).c
selected Led blinks continuously, with varying duty cycle,depending on the state of the button(polling).c
main.c
ADC10 repeatedly check pin and derive average value.c
Car Last Question.c

